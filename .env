# Clinic Settings
PORT=5000
CLINIC_ID=dr-white-32-dental
OWNER_EMAIL=drswhite32@gmail.com

# Optional: Google Gemini API Key (Backend will automatically switch to live Gemini 2.0 when key is present, otherwise falls back gracefully to smart clinic knowledge assistant)
GEMINI_API_KEY=

# Optional: SMTP Email Credentials for live email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
